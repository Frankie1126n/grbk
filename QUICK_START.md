# 🚀 快速启动指南

## 系统要求

- **Java**: JDK 1.8+
- **Node.js**: v6.0.0+
- **MySQL**: 8.0+
- **Maven**: 3.6+
- **NPM**: 3.0.0+

## 启动步骤

### 第一步：数据库准备

```bash
# 1. 登录MySQL
mysql -u root -p

# 2. 创建数据库并导入SQL脚本
source d:/javadm/grbk/database/blog_system.sql

# 或者直接执行
mysql -u root -p < d:/javadm/grbk/database/blog_system.sql
```

### 第二步：后端启动

```bash
# 进入后端目录
cd d:/javadm/grbk/backend

# 修改数据库配置（src/main/resources/application.yml）
# spring.datasource.username=your_username  # 修改为你的MySQL用户名
# spring.datasource.password=your_password  # 修改为你的MySQL密码

# 方式1: 使用Maven启动
mvn clean install
mvn spring-boot:run

# 方式2: 使用IDE
# 直接运行 src/main/java/com/blog/BlogApplication.java

# 启动成功后访问:
# API地址: http://localhost:8080/api
# Swagger文档: http://localhost:8080/api/swagger-ui/
```

### 第三步：前端启动

```bash
# 进入前端目录
cd d:/javadm/grbk/frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 启动成功后访问:
# 前端地址: http://localhost:8081
```

## 测试账号

系统已预置测试账号，可直接登录：

- **用户名**: admin
- **密码**: 123456
- **邮箱**: admin@blog.com

或

- **用户名**: testuser
- **密码**: 123456
- **邮箱**: test@blog.com

## 功能测试

### 1. 用户登录
- 访问 http://localhost:8081
- 输入用户名: admin, 密码: 123456
- 点击登录

### 2. 浏览文章
- 登录后自动跳转到首页
- 查看文章列表
- 点击文章卡片查看详情
- 使用搜索框搜索文章

### 3. 分类筛选
- 在右侧边栏点击分类
- 查看该分类下的文章

### 4. 查看详情
- 点击任意文章进入详情页
- 查看完整内容和标签
- 阅读量自动+1

## 常见问题

### Q1: 后端启动失败？
**A**: 检查以下几点：
1. MySQL服务是否启动
2. 数据库配置是否正确
3. JDK版本是否为1.8+
4. 端口8080是否被占用

### Q2: 前端启动失败？
**A**: 检查以下几点：
1. Node.js版本是否满足要求
2. 是否执行了 `npm install`
3. 端口8081是否被占用
4. 网络连接是否正常（下载依赖）

### Q3: 登录后401错误？
**A**: 
1. 检查后端是否正常运行
2. 检查JWT配置是否正确
3. 清除浏览器localStorage重新登录

### Q4: 跨域错误？
**A**: 
1. 确认后端CORS配置已生效
2. 检查前端axios baseURL配置
3. 确认请求头是否正确

## 项目结构说明

```
grbk/
├── database/          # 数据库SQL文件
├── backend/           # Spring Boot后端 (端口: 8080)
└── frontend/          # Vue2前端 (端口: 8081)
```

## API测试

使用Swagger进行API测试：
http://localhost:8080/api/swagger-ui/

或使用Postman/Apifox导入接口文档测试。

## 下一步

1. 尝试注册新用户
2. 发布新文章（需要登录）
3. 添加分类和标签
4. 自定义样式和功能

## 技术支持

如遇到问题，请检查：
1. 控制台错误日志
2. 浏览器开发者工具Network
3. 后端日志输出

祝使用愉快！🎉
