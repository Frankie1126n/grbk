<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.CategoryMapper">

    <!-- 查询所有分类（含文章数量） -->
    <select id="selectCategoryWithCount" resultType="com.blog.entity.Category">
        SELECT
            c.id,
            c.name,
            c.create_time,
            COUNT(b.id) AS blog_count,
            COUNT(b.id) AS article_count
        FROM category c
        LEFT JOIN blog b ON c.id = b.category_id AND b.publish_status = 1 AND b.is_deleted = 0
        GROUP BY c.id, c.name, c.create_time
        ORDER BY c.create_time DESC
    </select>

</mapper>
